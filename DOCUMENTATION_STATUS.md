# Documentation Status

The LISA Scheduler documentation lives in `lisa-ha-docs/`.

Current focus:

- Describe the integration as an event-driven Home Assistant scheduler.
- Keep heating, lighting, and notifications as automation examples only.
- Use the `lisa_scheduler` domain in current examples.
- Keep scraper guidance centered on `ScheduleScraper` and `ConfigurableScraper`.

Historical migration notes may still mention old `zhc_heating_scheduler` names where they are needed to help users upgrade.
