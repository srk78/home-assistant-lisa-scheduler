{
  "config": {
    "step": {
      "user": {
        "title": "LISA Scheduler",
        "description": "Set up the event scheduler. It will fire Home Assistant events when your club's schedule starts and ends, so you can trigger automations for heating, lighting, screens, or anything else.",
        "data": {
          "schedule_url": "Schedule URL",
          "logo_url": "Club logo URL (optional)",
          "pre_event_triggers": "Pre-event trigger times in minutes (comma-separated, e.g. 120, 30)",
          "pre_first_event_triggers": "Minutes before first event of day (comma-separated, optional)",
          "pre_last_event_end_triggers": "Minutes before last event ends (comma-separated, optional)",
          "post_last_event_triggers": "Minutes after last event ends (comma-separated, optional)",
          "scan_interval": "Schedule refresh interval (seconds)",
          "dry_run": "Dry run mode (log only, don't fire events)"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the schedule URL",
      "invalid_url": "Invalid schedule URL",
      "invalid_triggers": "Invalid trigger times — use comma-separated minutes, e.g. 120, 30",
      "unknown": "Unexpected error occurred"
    },
    "abort": {
      "already_configured": "This integration is already configured"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "LISA Scheduler Options",
        "data": {
          "logo_url": "Club logo URL (optional)",
          "pre_event_triggers": "Pre-event trigger times in minutes (comma-separated, e.g. 120, 30)",
          "pre_first_event_triggers": "Minutes before first event of day (comma-separated, optional)",
          "pre_last_event_end_triggers": "Minutes before last event ends (comma-separated, optional)",
          "post_last_event_triggers": "Minutes after last event ends (comma-separated, optional)",
          "scan_interval": "Schedule refresh interval (seconds)",
          "enabled": "Enable scheduler",
          "dry_run": "Dry run mode (log only, don't fire events)"
        }
      }
    }
  },
  "services": {
    "refresh_schedule": {
      "name": "Refresh Schedule",
      "description": "Force a refresh of the event schedule from the website."
    },
    "enable": {
      "name": "Enable Scheduler",
      "description": "Enable the scheduler so it fires events."
    },
    "disable": {
      "name": "Disable Scheduler",
      "description": "Disable the scheduler. No events will be fired while disabled."
    },
    "set_override": {
      "name": "Set Override",
      "description": "Manually activate the window for a given time period, firing the normal window/event HA events.",
      "fields": {
        "start_time": {
          "name": "Start Time",
          "description": "When to start the override window (ISO format)"
        },
        "end_time": {
          "name": "End Time",
          "description": "When to end the override window (ISO format)"
        }
      }
    },
    "clear_override": {
      "name": "Clear Override",
      "description": "Clear any manual override and resume normal scheduling."
    }
  }
}
